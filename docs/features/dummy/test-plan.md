Instead of writing tests immediately, define:

Given:
Feed A

When:
Scheduler runs

Then:
New items are inserted

And:
Duplicates are ignored.

Now Codex can generate both implementation and tests from the same source.